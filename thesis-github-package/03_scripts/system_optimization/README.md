# System Optimization Scripts

Place environment-tuning scripts here (for stable benchmark execution), such as:

- `optimize_system.sh`
- `restore_system.sh`

Also document:

- Which settings are changed
- How to restore defaults after experiments

