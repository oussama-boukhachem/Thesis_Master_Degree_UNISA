# Benchmark Sources

Place benchmark source files here, grouped by commit, preserving Ignite module structure when possible.

Suggested layout:

- `commit1_b038730/`
- `commit2_feba953/`
- `commit3_160dab0/`

For each commit, include both:

- Handwritten benchmark classes
- AI-generated benchmark classes

Recommended note:

If a benchmark was excluded (e.g., unstable/freeze cases), document it in a per-commit note file.

